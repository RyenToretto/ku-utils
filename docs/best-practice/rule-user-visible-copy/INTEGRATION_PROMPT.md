# 接入 Prompt：rule-user-visible-copy

请落地用户可见文案规范（来源：ku-utils `docs/best-practice/rule-user-visible-copy`）。

## 目标

1. 添加 `.cursor/rules/user-visible-copy.mdc`。
2. 列出 sink 与禁止类别；与任务台账/wait 文档交叉引用。
3. grep 业务文案中的「待后端」「一期」「waitRD」等，列整改项。

## 完成后

给出 rule 路径与违规命中摘要。
