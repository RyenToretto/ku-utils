# 接入 Prompt：rule-skill-entity-selector

请落地实体选择器最佳实践（来源：ku-utils `docs/best-practice/rule-skill-entity-selector`）。

## 目标

1. 添加 `.cursor/rules/entity-selector.mdc`（globs 含 `*Selector*.vue`、`DialogSelect*.vue`）。
2. 写明：防漏判定、List 双模金标、命名、禁止项、Demo 义务。
3. 可选 `.cursor/skills/selector/SKILL.md`：新建选择器步骤清单。
4. 对照现有选择器：标出「Dialog 内完整表」债项，新代码禁止再增。

## 完成后

列出 rule/skill 路径、现有选择器合规数 / 待整改数。
