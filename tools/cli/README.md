# @ku-utils/cli

> 团队 CLI 工具，提供项目初始化、创建与诊断命令。

## 安装

```bash
pnpm add -D @ku-utils/cli
```

## 命令

```bash
npx ku-utils init     # 初始化项目配置
npx ku-utils create <name>   # 创建新项目
npx ku-utils doctor   # 环境诊断
```

## 安装源

发布于 npmjs.org，直接 `pnpm add` 即可。

---

详细文档请参考 [ku-utils 文档站](../../docs/)
