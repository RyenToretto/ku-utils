---
description: "@ku-utils/eslint-config 使用规范。配置 ESLint 时适用，约束按场景选择正确的配置预设。"
globs:
alwaysApply: false
---

# @ku-utils/eslint-config 使用规范

## 核心原则

**按项目技术栈选对应预设**，禁止使用错误的预设（如 Vue 2 项目用 vue3 配置）。

## 预设选择

| 项目类型 | 导入路径 |
|---|---|
| 纯 JS/TS（非框架） | `@ku-utils/eslint-config/base` |
| Vue 2 项目 | `@ku-utils/eslint-config/vue2` |
| Vue 3 / Vite 项目 | `@ku-utils/eslint-config/vue3` |
| Nuxt 4 项目 | `@ku-utils/eslint-config/nuxt4` |

## 使用方式（ESLint v9 Flat Config）

```js
// eslint.config.js
import vue3Config from '@ku-utils/eslint-config/vue3'

export default [
  ...vue3Config,
  {
    ignores: ['**/dist/**', '**/node_modules/**'],
  },
  {
    files: ['**/*.vue'],
    rules: {
      'vue/block-lang': 'off',  // 按需覆盖规则
    },
  },
]
```

## 禁止事项

- 禁止用 `eslint-disable` 注释绕过规则（应修复或在配置层 override）
- 禁止在业务文件中用 `/* eslint-disable */` 全文禁用 lint
