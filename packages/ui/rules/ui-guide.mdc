---
description: "@ku-utils/ui 使用规范。Vue 3 项目中使用团队 UI 组件库时适用。"
globs:
alwaysApply: false
---

# @ku-utils/ui 使用规范

## 核心原则

**优先使用 `@ku-utils/ui` 中已有的组件**，禁止重复造功能相同的业务 UI 组件（如重写 Empty、Modal、StatusTag）。

## 安装与注册

```ts
// main.ts
import { install } from '@ku-utils/ui'
import '@ku-utils/ui/style'

app.use(install)
```

## 组件速查

```html
<!-- 空态展示 -->
<DuEmpty description="暂无数据" />
<DuEmpty description="加载失败" icon="error" />

<!-- 按钮 -->
<DuButton @click="handleClick">点击</DuButton>
<DuButton type="primary" :loading="isLoading">提交</DuButton>

<!-- 卡片 -->
<DuCard title="标题" :bordered="true">卡片内容</DuCard>

<!-- 状态标签 -->
<DuStatusTag status="success">成功</DuStatusTag>
<DuStatusTag status="warning">警告</DuStatusTag>
<DuStatusTag status="error">失败</DuStatusTag>

<!-- 弹窗 -->
<DuModal v-model:visible="dialogVisible" title="弹窗标题">
  弹窗内容
</DuModal>
```

## 禁止事项

- 禁止为已有组件的功能再写一个新组件（如自定义 `MyEmpty.vue`）
- 禁止直接修改 `@ku-utils/ui` 的组件文件 — 通过 props/slot 扩展
