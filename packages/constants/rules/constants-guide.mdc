---
description: "@ku-utils/constants 使用规范。在业务代码中使用 HTTP 状态码、分页默认值、文件限制、存储 Key 等常量时适用。"
globs:
alwaysApply: false
---

# @ku-utils/constants 使用规范

## 核心原则

**禁止硬编码常量**：HTTP 状态码、分页默认值、文件大小限制、存储 Key 等必须从 `@ku-utils/constants` 引用，禁止在业务代码中散落的魔法数字或字符串。

## 使用示例

```ts
import {
  HttpStatus, ContentType, RequestMethod,
  PAGINATION, FILE_SIZE_LIMIT, FILE_ACCEPT,
  StorageKey, REGEX
} from '@ku-utils/constants'

// HTTP 状态码
if (res.code === HttpStatus.UNAUTHORIZED) { /* 401 跳登录 */ }
if (res.code === HttpStatus.OK) { /* 200 成功 */ }

// 分页默认值
const { DEFAULT_PAGE, DEFAULT_PAGE_SIZE, PAGE_SIZES } = PAGINATION
// DEFAULT_PAGE = 1, DEFAULT_PAGE_SIZE = 10, PAGE_SIZES = [10, 20, 50, 100]

// 文件限制
if (file.size > FILE_SIZE_LIMIT.IMAGE) { /* 超过5MB */ }
const accept = FILE_ACCEPT.IMAGE  // '.jpg,.jpeg,.png,.gif,.webp,.svg'

// 存储 Key（localStorage/sessionStorage 统一用此枚举）
local.set(StorageKey.TOKEN, token)
local.get(StorageKey.USER_INFO)

// 正则
REGEX.EMAIL.test('user@do.com')    // true
REGEX.MOBILE.test('13812345678')   // true
```

## 禁止事项

- 禁止 `if (code === 401)` — 用 `HttpStatus.UNAUTHORIZED`
- 禁止 `pageSize: 10` — 用 `PAGINATION.DEFAULT_PAGE_SIZE`
- 禁止 `file.size > 5 * 1024 * 1024` — 用 `FILE_SIZE_LIMIT.IMAGE`
- 禁止自定义 localStorage key 字符串 — 用 `StorageKey.*`
- 禁止自写邮箱/手机号正则 — 用 `REGEX.EMAIL`/`REGEX.MOBILE`
