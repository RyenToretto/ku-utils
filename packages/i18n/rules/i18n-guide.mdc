---
description: "@ku-utils/i18n 使用规范。多语言国际化场景时适用，约束单例创建和字符串使用方式。"
globs:
alwaysApply: false
---

# @ku-utils/i18n 使用规范

## 核心原则

1. **所有用户可见字符串必须走 i18n**，禁止在模板/JS 中硬编码中文/英文
2. **createI18n 单例**：全局只创建一次，导出后复用
3. **locale 文件不得有重复 key**

## 用法

```ts
// src/plugins/i18n.ts
import { createI18n, detectBrowserLocale, mergeMessages } from '@ku-utils/i18n'

const i18n = createI18n({
  locale: detectBrowserLocale(['zh-CN', 'en']),
  fallbackLocale: 'zh-CN',
  messages: {
    'zh-CN': { greeting: '你好，{name}！', save: '保存' },
    'en': { greeting: 'Hello, {name}!', save: 'Save' },
  }
})

export default i18n
export const { t, setLocale } = i18n
```

```vue
<!-- 模板中使用 -->
<template>
  <el-button>{{ t('save') }}</el-button>
  <p>{{ t('greeting', { name: username }) }}</p>
</template>
```

## 禁止事项

- 禁止 `<button>保存</button>`（硬编码字符串）— 使用 `t('save')`
- 禁止多处 `createI18n()` 创建实例
- 禁止 locale 文件同级重复 key
