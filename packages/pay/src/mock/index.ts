import { mockOrderInfo, mockPayResult, mockSkuAll, mockUserInfo } from './responses';

export const doMock = {
  loginSuccess: '' as unknown,
  mockSkuAll,
  mockUserInfo,
  mockOrderInfo,
  mockPayResult,
};

export default doMock;
