---
description: "@ku-utils/pay 使用规范。接入 HelaPay 支付（匿名登录 / SKU / 微信 JSAPI）时适用。"
globs:
alwaysApply: false
---

# @ku-utils/pay 使用规范

## 核心原则

1. **单例**：全局只调用一次 `createHelaPay()`
2. **微信 JSAPI 必须在 wx.ready 回调内调用** `wxJSPay()`
3. **调用顺序**：`init()` → `requestUserInfo()` → `requestAllSkuList()` → `requestPayScan()` → 支付

## 用法

```ts
// src/plugins/pay.ts
import { createHelaPay } from '@ku-utils/pay'

export const helaPay = createHelaPay({
  host: 'https://pay.example.com',
  pkg: 'com.example.app',
})

// 支付流程
await helaPay.init({ pkg: 'com.example.app', host: '...' })
const user = await helaPay.requestUserInfo()
const skuList = await helaPay.requestAllSkuList()
const payInfo = await helaPay.requestPayScan(skuList[0].skuId)

// 微信 JSAPI 支付
wx.ready(() => {
  helaPay.wxJSPay(payInfo, {
    onSuccess: () => checkResult(),
    onFail: (err) => console.error(err),
  })
})
```

## 禁止事项

- 禁止在 `wx.ready` 回调外调用 `wxJSPay()`
- 禁止跳过 `init()` 直接调用其他方法
