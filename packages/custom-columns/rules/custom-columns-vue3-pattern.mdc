---
description: Vue3 自定义列开发模式规范（@ku-utils/custom-columns）。新 table 页面接入自定义列时适用，约束 schema 结构、接入方式、模板写法和禁止事项。
globs:
alwaysApply: false
---

# Vue3 自定义列开发模式规范

基于 `@ku-utils/custom-columns` 包（Vue3 + Element Plus），使用 **schema + v-for** 方案。

## 核心规则

新 table 页面通过 `useSchemaColumnConfig` composable + schema + v-for 接入自定义列。

## 接入步骤

### 1. 引入样式（入口文件一次）

```ts
// main.ts
import '@ku-utils/custom-columns/style'
```

### 2. 新建列 schema 文件（`*ColumnSchemas.ts`）

```ts
import type { ColumnSchema } from '@ku-utils/custom-columns'

// 叶子列必须有全局唯一且稳定的 prop
export const MY_COLUMN_SCHEMAS: ColumnSchema[] = [
  { prop: 'cost', label: '成本', minWidth: 110, align: 'right', renderType: 'float', renderArgs: [2, true], group: '效果指标' },
  { prop: 'rate', label: '转化率', minWidth: 100, align: 'right', renderType: 'percent', renderArgs: [1, true], group: '效果指标' },
  // 嵌套表头：父节点设 children，不设 prop
  { label: '转化层', children: [
    { prop: 'install', label: '激活', renderType: 'integer', group: '转化指标' }
  ]}
]
```

### 3. 页面 setup 调用 composable

```ts
import { useSchemaColumnConfig } from '@ku-utils/custom-columns'
import { MY_COLUMN_SCHEMAS } from './myColumnSchemas'

const { visibleSchemas, tableRenderKey, formatSchemaCell } = useSchemaColumnConfig({
  columnSchemas: MY_COLUMN_SCHEMAS,
  storageKey: 'my_module_col', // 全局唯一，勿与其他页面重复
  schemaVersion: 1,            // 破坏性变更（rename/delete prop）时递增
})
```

`useSchemaColumnConfig` 内部会 `provide` crud 上下文，供 `TableWrap` 内的 `DoTableHeader` 自动消费，无需手动挂载弹窗。

### 4. 模板 — 扁平列

```vue
<!-- TableWrap 必须加 enable-do-header :disabled-column-config="false" -->
<!-- el-table 必须绑定 :key="tableRenderKey" -->
<TableWrap enable-do-header :disabled-column-config="false">
  <el-table :key="tableRenderKey" :data="list" border>
    <!-- 固定列写在 v-for 外 -->
    <el-table-column prop="id" label="ID" fixed="left" />
    <!-- 可配置列 -->
    <el-table-column
      v-for="schema in visibleSchemas"
      :key="schema.prop"
      :prop="schema.prop"
      :label="schema.label"
      :min-width="schema.minWidth"
      :align="schema.align || 'left'"
      v-bind="schema.elAttrs || {}"
    >
      <template #default="{ row, column, $index }">
        <component :is="schema.cellComponent" v-if="schema.cellComponent"
          :row="row" :column="column" :index="$index" :schema="schema" />
        <span v-else>{{ formatSchemaCell(row[column.property], schema) }}</span>
      </template>
    </el-table-column>
  </el-table>
</TableWrap>
```

### 5. 模板 — 嵌套表头

```vue
<!-- 直接用包内置的递归 SchemaColumn 组件 -->
<SchemaColumn
  v-for="schema in visibleSchemas"
  :key="schema.prop || schema.label"
  :schema="schema"
  :format-cell="formatSchemaCell"
/>
```

## renderType 速查

| renderType | renderArgs | 说明 |
|---|---|---|
| `text`（默认） | 无 | 原始文本 |
| `integer` | 无 | 整数（千分位） |
| `float` | `[digits=2, noZero=true]` | 浮点数（千分位） |
| `percent` | `[digits=1, noZero=true]` | 百分比 |

## 自定义单元格（cellComponent）

- **推荐**：外部 `.vue` SFC（`<script setup>`），有 scoped style、IDE 跳转
- Props 固定为：`row`, `column`, `index`, `schema`

## 版本管理规则

- 字段**新增**：不递增 `schemaVersion`（新 prop 不影响已有配置）
- 字段**重命名 / 删除**：必须递增 `schemaVersion`（触发旧缓存自动失效）
- `schemaVersion` 为 number，从 `1` 起递增

## 禁止事项

- `el-table` 必须绑定 `:key="tableRenderKey"`，否则列顺序变更不生效
- `storageKey` 在多个页面间必须全局唯一，重复会导致配置互相覆盖
- 固定列（`alwaysVisible`）写在 `v-for` 外，不能放进可配置的 `columnSchemas`
- 每个叶子 schema 的 `prop` 必须全局唯一且稳定（重命名必须同步递增 `schemaVersion`）
