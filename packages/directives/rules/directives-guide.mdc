---
description: "@ku-utils/directives 使用规范。Vue 3 项目中使用自定义指令（权限、加载、复制、防抖等）时适用。"
globs:
alwaysApply: false
---

# @ku-utils/directives 使用规范

## 核心原则

**`installDirectives` 批量全局注册**，不要逐个注册；**权限控制必须用 `v-permission`**，禁止在业务代码中散落的 `userInfo.roles.includes()` 判断。

## 注册方式

```ts
// main.ts
import { installDirectives } from '@ku-utils/directives'
app.use(installDirectives)
```

## 指令速查

```html
<!-- 权限控制（RBAC）-->
<el-button v-permission="'admin'">管理员功能</el-button>
<el-button v-permission="['admin', 'editor']">任一角色</el-button>

<!-- 加载状态 -->
<div v-loading="isLoading">内容区域</div>

<!-- 点击外部 -->
<div v-click-outside="handleClose">弹出层</div>

<!-- 一键复制 -->
<span v-copy="'复制这段文字'">点击复制</span>

<!-- 防抖点击（500ms） -->
<el-button v-debounce:500="handleSubmit">提交</el-button>

<!-- 图片懒加载 -->
<img v-lazy-load="imageUrl" />

<!-- 长按（700ms） -->
<div v-longpress="handleLongPress">长按触发</div>

<!-- Tooltip -->
<span v-tooltip="'提示文字'">悬停说明</span>
```

## 禁止事项

- 禁止 `if (roles.includes('admin'))` 控制 UI 显示 — 使用 `v-permission`
- 禁止手动 `addEventListener('click', ...)` 实现点外关闭 — 使用 `v-click-outside`
- 禁止 `setTimeout` 实现防抖提交 — 使用 `v-debounce`
