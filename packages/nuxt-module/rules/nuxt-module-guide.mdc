---
description: "@ku-utils/nuxt-module 使用规范。Nuxt 4 项目中集成 ku-utils 工具链时适用。"
globs:
alwaysApply: false
---

# @ku-utils/nuxt-module 使用规范

## 核心原则

1. **通过模块 auto-import**：`@ku-utils/hooks` 的 composables 由模块自动注册，禁止手动 `import { useRequest } from '@ku-utils/hooks'`
2. **configKey 为 `kuUtils`**：在 `nuxt.config.ts` 中配置 `kuUtils: {}` 选项
3. **SSR 安全**：模块已配置 `@ku-utils/hooks` 的 SSR transpile，无需手动添加

## 配置

```ts
// nuxt.config.ts
export default defineNuxtConfig({
  modules: ['@ku-utils/nuxt-module'],
  kuUtils: {
    components: true,    // 自动注册 DuButton/DuEmpty 等组件
    composables: true,   // 自动导入 useRequest/usePagination 等
    directives: true,    // 自动注册 v-permission/v-loading 等
    css: true,           // 自动引入 design-tokens + ui CSS
  },
})
```

## 禁止事项

- 禁止在 Nuxt 项目中手动 `import { useRequest } from '@ku-utils/hooks'`（已 auto-import）
- 禁止手动添加 `@ku-utils/hooks` 到 `build.transpile`（模块已处理）
- 禁止重复注册 ku-utils 组件（模块已全局注册）
