---
description: "@ku-utils/types 使用规范。定义 TypeScript 类型时适用，优先复用共享类型而非重复定义。"
globs:
alwaysApply: false
---

# @ku-utils/types 使用规范

## 核心原则

**优先复用**：在定义 `T | null`、API 响应类型、分页参数等结构前，先检查 `@ku-utils/types` 是否已有对应类型。

## 必须使用 `import type`

```ts
// ✅ 正确
import type { Nullable, ApiResponse, SelectOption } from '@ku-utils/types'

// ❌ 错误（值导入类型）
import { Nullable } from '@ku-utils/types'
```

## 高频类型速查

```ts
import type {
  Nullable, MaybeRef, Recordable, DeepPartial,
  ApiResponse, PaginatedResponse, PaginationParams,
  SelectOption, TreeNode, MenuItem
} from '@ku-utils/types'

// T | null
const val: Nullable<string> = null

// T | Ref<T>（Vue 3 composable 参数常用）
function useX(input: MaybeRef<string>) {}

// Record<string, T>
const map: Recordable<number> = {}

// 深度 Partial
const patch: DeepPartial<UserInfo> = {}

// API 响应
const res: ApiResponse<User> = { code: 200, data: user, message: '', success: true }

// 分页响应
const list: PaginatedResponse<Item>

// 下拉选项
const options: SelectOption[] = [{ label: '选项A', value: 1 }]
```

## 禁止事项

- 禁止 `T | null | undefined` — 使用 `Nullable<T>`
- 禁止自定义 `{ code, data, message }` 接口 — 使用 `ApiResponse<T>`
- 禁止重复定义 `SelectOption`/`TreeNode` 等通用结构
