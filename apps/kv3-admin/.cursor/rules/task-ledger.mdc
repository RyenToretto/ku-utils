---
description: kv3-admin 任务台账（精简）— owner 序号 ID、按人/状态分节活跃看板
globs:
  - docs/task/**
alwaysApply: false
---

# 任务台账（kv3-admin · 精简）

> 最佳实践：`docs/best-practice/rule-task-ledger/`（完整版含 wait 双队列；本仓 starter 用精简落地）。  
> 台账目录：`apps/kv3-admin/docs/task/`。

## 何时登记

用户说「实现 / 开发 / 优化 / 修复」且触及 **kv3-admin 业务页/组件/Mock** → **先**建 `docs/task/{id}.md` 并（若仍活跃）写入 `TASK.md` **本人章节**，**再**编码。  
纯文档/rule 同步、单行 typo 可跳过。

## 任务 ID（多人防冲突 · 强制）

**禁止**全局递增 `P1` / `P2`。

| 规则 | 说明 |
| --- | --- |
| 格式 | `{owner}{seq}`，全小写，无分隔符 |
| `owner` | `git config user.name` 转小写并去掉非 `[a-z0-9]`；也可用 GitHub login |
| `seq` | 扫 `docs/task/{owner}*.md` 的最大序号 + 1（**以文件为准**） |
| 示例 | `koujianfeng1`、`koujianfeng2`、`alice3` |

## `TASK.md`：按人 → 按状态（降冲突 + 防膨胀）

| 真源 | 职责 |
| --- | --- |
| `docs/task/{id}.md` | **全量档案**；历史一律保留 |
| `TASK.md` | **仅活跃看板**；结构固定如下 |

```markdown
## {owner}
### ⭕️ 待实现
| ID | 标题 | 路径 |
| --- | --- | --- |
| koujianfeng1 | … | ./koujianfeng1.md |

### ⚠️ 部分完成
| ID | 标题 | 路径 |
| --- | --- | --- |

### ⛔️ 阻塞
| ID | 标题 | 路径 |
| --- | --- | --- |
```

硬约束：

1. **只改自己的 `## {owner}` 节**；没有则在文件末尾新建该节（勿插进别人中间）
2. 行只出现在对应状态子节；状态变更 = **在本人节内剪切行**到另一子节（或出看板）
3. `✅` / `❎` → 更新 `{id}.md` 后 **从 `TASK.md` 删行**（不进索引）
4. 禁止改别人的节、禁止全局重排、禁止把历史堆回 `TASK.md`

合并冲突：通常只撞在同一人同状态表；保留双方新增行即可。

## 状态符号（禁止 `- [ ]`）

| 符号 | 含义 | 是否进 `TASK.md` |
| --- | --- | --- |
| `⭕️` | 待实现 | 是 → `### ⭕️ 待实现` |
| `⚠️` | 部分完成 | 是 → `### ⚠️ 部分完成` |
| `⛔️` | 阻塞（须附原因） | 是 → `### ⛔️ 阻塞` |
| `✅` | 已完成 | 否（仅 `{id}.md`） |
| `❎` | 确认先不做 / 非本仓义务 | 否（仅 `{id}.md`） |

一次任务尽量一次 commit；收工时同步：更新 `{id}.md` + 从本人节移除。

## 与完整版差异

本仓不强制 waitRD/waitConfirm 双队列与上游合同增量 Skill（见参考模块 `rule-fe-be-doc-sync`）。需要时再升格。
