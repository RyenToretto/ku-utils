---
description: kv3-admin API 合同 — requestXxx、信封 code:0/data、$MAPS、禁兼容
globs:
  - '**/_api/**'
  - '**/_map/**'
  - src/plugins/axios.ts
  - src/mock/**
alwaysApply: false
---

# API / 字典 / 响应示例（kv3-admin）

> 最佳实践：`docs/best-practice/rule-api-contract/`。  
> Mock 加载边界见 `mock-isolation.mdc`（须同形）。

## 本仓信封（冻结）

| 项 | 字面量 |
| --- | --- |
| 成功 | `code === 0`（拦截器亦认 `'0'` 同码字符串态） |
| 载荷 | `data` |
| 分页常见 | `data.lists` + `data.total` |
| 工具 | `@/mock/utils` 的 `ok` / `pageOk` |

**禁止**认海外仓 `'0000'`/`result` 等异合同；**禁止**双字段兜底（`message`/`msg`、`id`/`userId`）。

## HTTP 入口

- 业务唯一实现在 `modules/.../_api`；页面只调 **`requestXxx`**
- 禁页面散落 URL / 拼 wire payload；禁 `api/common` 对业务 `_api` 二次 re-export 当共用出口
- GET 数组交给序列化；禁页面 `.join(',')`

## `$MAPS` / 字典

- 不走接口的稳定码表 → `_map` 聚合进 `$MAPS` 再引用
- 有 enums/options 专用接口 → 必须 `_api` 请求；空/失败禁伪造下拉
- 禁页面 magic string 作主真源

## 响应示例边界

文档 JSON「响应示例」**只读形状**（键名、分页、信封），**不得**把示例枚举项/label 抄进 `$MAPS` / 页面 options。  
真源优先级：已确认合同 → 后端已落地 → **运行时字典接口** → Mock → 文档示例（只读形状）。

## Agent 自检（写选项前）

1. value/label 来自运行时接口、已确认合同，还是文档示例？示例 → 禁止入码为字典。
2. 是否存在专用选项 API？有则必须请求。
