---
description: "@ku-utils/hooks 使用规范。Vue 3 项目中处理异步请求、分页、剪贴板、定时器等场景时适用。"
globs:
alwaysApply: false
---

# @ku-utils/hooks 使用规范

## 核心原则

**优先使用 hooks 替代手写响应式逻辑**：`useRequest`、`usePagination`、`useClipboard` 等已封装了 loading/error/cleanup，禁止在业务代码中重复实现。

## 高频场景速查

### 异步请求

```ts
// ✅ 正确
const { data, loading, error, execute } = useRequest(() => getUserList())
// 立即执行
const { data } = useRequest(() => getUserList(), { immediate: true })

// ❌ 错误 — 手写 loading/error
const loading = ref(false)
const data = ref(null)
async function load() { loading.value = true; /* ... */ }
```

### 分页列表

```ts
// ✅ 正确
const { list, total, page, pageSize, loading, fetch, changePage, changePageSize } =
  usePagination(({ page, pageSize }) => getList({ page, pageSize }))

// ❌ 错误 — 手写 page/pageSize/loading 状态
```

### 定时器

```ts
// ✅ 正确（组件卸载时自动清理）
// 返回 { isActive, start, stop }，不是 { pause, resume }
const { isActive, stop } = useInterval(() => checkStatus(), 5000)

// ❌ 错误 — 手写 setInterval + onUnmounted 清理
```

### 倒计时

```ts
// ✅ 正确
// 返回 { count, isActive, start, stop, reset }，不是 { remaining, isRunning }
const { count, isActive, start } = useCountdown(60)

// ❌ 错误
const { remaining, isRunning } = useCountdown(60)  // remaining/isRunning 不存在
```

### 弹窗状态

```ts
// ✅ 正确
// 返回 { visible, show, hide, toggle }，不是 { open, close }
const { visible, show, hide } = useDialogState()

// ❌ 错误
const { open, close } = useDialogState()  // open/close 不存在，应用 show/hide
```

### 断点检测

```ts
// ✅ 正确
// 返回 { sm, md, lg, xl, xxl }（布尔 Ref）
const { sm, md } = useBreakpoint()
const isMobile = computed(() => !md.value)

// ❌ 错误 — isMobile/isTablet/current 不存在
const { isMobile } = useBreakpoint()   // isMobile 不在 useBreakpoint 返回值中
// ✅ 设备检测（SSR 安全）用 useDeviceDetect
const { isMobile } = useDeviceDetect()
```

### 表格 max-height

```ts
// ✅ 正确 — 本仓封装（contain 为页面根 .page-*；滚动容器 .domain-module-main，Demo 回落 .main-container-inner；扣底 padding）
import { useAdminTableMaxHeight } from '@/composables/useAdminTableMaxHeight'
const maxHeight = useAdminTableMaxHeight('.page-simple-example-list', 400)
// <el-table :max-height="maxHeight" class="do-inner-scroller page-table hide-table-border" ... />

// ❌ 错误 — 无参 useMaxHeight() 默认找 .main-body，本仓不存在，高度永不重算
import { useMaxHeight } from '@ku-utils/hooks'
const maxHeight = useMaxHeight()
```

## 禁止事项

- 禁止在 `setup()` 外部（如普通函数、生命周期之外）调用 composable
- 禁止手动管理 loading/error 状态 — 使用 `useRequest`
- 禁止手写 setInterval + 手动 clearInterval — 使用 `useInterval`
- 禁止 `useInterval` 返回值解构为 `pause/resume` — 正确字段是 `start/stop`
- 禁止 `useCountdown` 解构 `remaining/isRunning` — 正确字段是 `count/isActive`
- 禁止 `useDialogState` 解构 `open/close` — 正确方法名是 `show/hide`
- 禁止页面级 `el-table` 省略 `:max-height`；禁止无参 `useMaxHeight()`（须走 `useAdminTableMaxHeight('.page-xxx')`）
